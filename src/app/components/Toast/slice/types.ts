/* --- STATE --- */
export interface ToastState {
  openSuccessToast: boolean;
  titleSuccessToast: string;
  messageSuccessToast: string;
  openErrorToast: boolean;
  titleErrorToast: string;
  messageErrorToast: string;
}
